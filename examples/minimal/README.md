# Minimal PromptABI example

This example wires a tiny chat-template bundle, a tool definition, and a JSON
answer schema into `promptabi verify`.

```bash
promptabi verify --config examples/minimal/promptabi.json
```

