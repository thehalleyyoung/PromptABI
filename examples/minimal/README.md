# Minimal PromptABI example

This example wires prompt segments, a tool definition, and a JSON answer schema
into `promptabi verify` using the typed artifact config shape.

```bash
promptabi verify --config examples/minimal/promptabi.json
```
